info:
  name: List sport type
  type: http
  seq: 3

http:
  method: GET
  url: "{{url_local}}/line/list/sport/type"
  auth:
    type: bearer
    token: "{{tokenLine}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
