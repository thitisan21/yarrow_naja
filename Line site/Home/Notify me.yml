info:
  name: Notify me
  type: http
  seq: 13

http:
  method: POST
  url: "{{url_local}}/line/notify/event"
  body:
    type: json
    data: |-
      {
          "id": "695b558c33baa1e6bcb3553e",
          "isFucking": true
      }
  auth:
    type: bearer
    token: "{{tokenLine}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
