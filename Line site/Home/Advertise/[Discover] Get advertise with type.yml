info:
  name: "[Discover] Get advertise with type"
  type: http
  seq: 1

http:
  method: GET
  url: "{{url_local}}/line/ads/list?bannerPosition=discover"
  params:
    - name: bannerPosition
      value: discover
      type: query
      description: // discover, payment
  auth:
    type: bearer
    token: "{{tokenLine}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
