info:
  name: Check promotion can use
  type: http
  seq: 15

http:
  method: GET
  url: "{{url_local}}/line/promotion/check"
  body:
    type: json
    data: |-
      {
        "eventId": "69d869d8cbd56b67a8d86001",
        "promotionCode": "FULLCODE2"
      }
  auth:
    type: bearer
    token: "{{tokenLine}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
