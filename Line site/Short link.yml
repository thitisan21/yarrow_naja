info:
  name: Short link
  type: http
  seq: 5

http:
  method: GET
  url: "{{url_local}}/sl/:shortLink"
  params:
    - name: shortLink
      value: jiAb9gBP9
      type: path
  auth:
    type: bearer
    token: "{{tokenLine}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
