info:
  name: Upsert bank account
  type: http
  seq: 1

http:
  method: POST
  url: "{{url_local}}/line/user/bank/account"
  body:
    type: json
    data: |-
      {
          "accountName": "สมหมาย งามเิศ", 
          "bankName": "SCB", // SCB, KBANK, PROMPTPAY
          "bankAccountNumber": "123456788" 
      }
  auth:
    type: bearer
    token: "{{tokenLine}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
