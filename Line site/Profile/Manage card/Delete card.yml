info:
  name: Delete card
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{url_local}}/line/user/card/:id"
  params:
    - name: id
      value: 67bc304e833761dfdc080db0
      type: path
  auth:
    type: bearer
    token: "{{tokenLine}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
