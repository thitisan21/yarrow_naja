info:
  name: Forgot password
  type: http
  seq: 3

http:
  method: POST
  url: "{{url_local}}/auth/forgotPassword"
  body:
    type: json
    data: |-
      {
          "email": "a.mod_pumppb@hotmail.co.th"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
