info:
  name: Logout
  type: http
  seq: 5

http:
  method: GET
  url: "{{url_local}}/auth/logout"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
