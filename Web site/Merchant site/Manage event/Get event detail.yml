info:
  name: Get event detail
  type: http
  seq: 4

http:
  method: GET
  url: "{{url_local}}/backoffice/event/:id"
  params:
    - name: id
      value: 69a7a8e1cd034109d8dadcb7
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
