info:
  name: Get promotion info
  type: http
  seq: 7

http:
  method: GET
  url: "{{url_local}}/backoffice/promotion/detail/:promotionId"
  params:
    - name: promotionId
      value: 69b7a428efbdce566fc29f6b
      type: path
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  status => all, open, pause, expired, cancel
  search => promoTitle
  sortBy => promoTitle, promoDateEnd, createdAt
