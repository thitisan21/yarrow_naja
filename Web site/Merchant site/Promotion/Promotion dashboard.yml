info:
  name: Promotion dashboard
  type: http
  seq: 6

http:
  method: GET
  url: "{{url_local}}/backoffice/promotion/dashboard"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
