info:
  name: "[Export] List promotion code"
  type: http
  seq: 9

http:
  method: GET
  url: "{{url_local}}/backoffice/promotion/export/list/code/:promotionId?status=all&search&sortBy=createdAt&sortOrderBy=desc"
  params:
    - name: status
      value: all
      type: query
    - name: search
      value: ""
      type: query
    - name: sortBy
      value: createdAt
      type: query
    - name: sortOrderBy
      value: desc
      type: query
    - name: promotionId
      value: 69b7a428efbdce566fc29f6b
      type: path
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  status => all, redeemed, rejected
  search => createdBy
  sortBy => status, createdAt, createdBy
