info:
  name: Export invoice pdf
  type: http
  seq: 4

http:
  method: GET
  url: "{{url_local}}/backoffice/export/event/invoice/:id"
  params:
    - name: id
      value: 6825a39d2e0e6a2cce83bde1
      type: path
      description: // 314
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
