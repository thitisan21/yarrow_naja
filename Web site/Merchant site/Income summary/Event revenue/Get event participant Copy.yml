info:
  name: Get event participant Copy
  type: http
  seq: 3

http:
  method: GET
  url: "{{url_local}}/backoffice/event/participant/:id?skip=1&limit=10&type=participant&sortBy=name&sortOrderBy=asc"
  params:
    - name: skip
      value: "1"
      type: query
    - name: limit
      value: "10"
      type: query
    - name: type
      value: participant
      type: query
      description: // participant, leave
    - name: search
      value: kenny
      type: query
      description: "// optional => displayname, name "
      disabled: true
    - name: sortBy
      value: name
      type: query
      description: // name, status, createdAt, paymentAmount
    - name: sortOrderBy
      value: asc
      type: query
      description: // asc, desc
    - name: id
      value: 668b61301e6fc67f5e8a3d1a
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
