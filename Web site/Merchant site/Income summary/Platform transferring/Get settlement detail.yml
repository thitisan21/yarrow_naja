info:
  name: Get settlement detail
  type: http
  seq: 2

http:
  method: GET
  url: "{{url_local}}/backoffice/merchant/settlement/detail/:id"
  params:
    - name: id
      value: 66b9cbb6890ef55d49baed9a
      type: path
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
