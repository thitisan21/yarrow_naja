info:
  name: List of province
  type: http
  seq: 2

http:
  method: GET
  url: "{{url_local}}/backoffice/merchant/location/province?search="
  params:
    - name: search
      value: ""
      type: query
      description: // location
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
