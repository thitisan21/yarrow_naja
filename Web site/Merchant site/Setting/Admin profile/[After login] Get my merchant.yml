info:
  name: "[After login] Get my merchant"
  type: http
  seq: 1

http:
  method: GET
  url: "{{url_local}}/backoffice/my-merchant"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
