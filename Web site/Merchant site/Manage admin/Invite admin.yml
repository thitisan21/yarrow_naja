info:
  name: Invite admin
  type: http
  seq: 1

http:
  method: POST
  url: "{{url_local}}/backoffice/merchant/invite/admin"
  body:
    type: json
    data: |-
      {
          "email": "ken.nuttapus@gmail.com",
          "name": "Kenny1",
          "phoneNumber": "0899999999",
          "role": "ADMIN" //ADMIN, STAFF
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
