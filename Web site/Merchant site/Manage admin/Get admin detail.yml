info:
  name: Get admin detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{url_local}}/backoffice/merchant/admin/:id"
  params:
    - name: id
      value: 66b2f99502507b9159398642
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
