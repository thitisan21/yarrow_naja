info:
  name: Get admin detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{url_local}}/backoffice/admin/:id"
  params:
    - name: id
      value: 66b1e319d0e548210fb9c00a
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
