info:
  name: Update merchant detail
  type: http
  seq: 12

http:
  method: PATCH
  url: "{{url_local}}/backoffice/admin/merchant/detail/:id"
  params:
    - name: id
      value: 668b82eb02507b91593985fc
      type: path
  body:
    type: json
    data: |-
      {
          "password": "Aa123456", //madatory
          "merchantName": "test event for mom 2", //optional
          "commissionRate": 15, //optional
          "payround": "WEEKLY" //optional ex. WEEKLY, 
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
