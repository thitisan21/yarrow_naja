info:
  name: Get event detail
  type: http
  seq: 9

http:
  method: GET
  url: "{{url_local}}/backoffice/merchant/info/event/detail/:id"
  params:
    - name: id
      value: 668b61301e6fc67f5e8a3d1a
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
