info:
  name: Get detail refund
  type: http
  seq: 11

http:
  method: GET
  url: "{{url_local}}/backoffice/admin/refund/detail/:id"
  params:
    - name: id
      value: 66b58cf0621fe82a094fabb0
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
