info:
  name: "[Export] List merchant"
  type: http
  seq: 3

http:
  method: GET
  url: "{{url_local}}/backoffice/export/merchant/list?status=all&dateStart=2024-07-01&dateEnd=2024-09-01"
  params:
    - name: status
      value: all
      type: query
      description: // all, pending, request, open, paused, delete
    - name: search
      value: ""
      type: query
      description: // merchantName
      disabled: true
    - name: dateStart
      value: 2024-07-01
      type: query
      description: // YYYY-MM-DD
    - name: dateEnd
      value: 2024-09-01
      type: query
      description: // YYYY-MM-DD
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
