info:
  name: Create merchant
  type: http
  seq: 1

http:
  method: POST
  url: "{{url_local}}/backoffice/merchant"
  body:
    type: json
    data: |-
      {
          "merchantName": "Dev kenny2",
          "sportType": [
              "basketball"
          ],
          "commissionRate": "5",
          "payround": "MONTHLY",
          "email": "ken.nuttapus@gmail.com"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
