info:
  name: Export settlement
  type: http
  seq: 4

http:
  method: GET
  url: "{{url_local}}/backoffice/settlement/export/list?status=overdue&dateStart=2025-01-01&dateEnd=2025-09-01"
  params:
    - name: status
      value: overdue
      type: query
      description: all, pending, paymented, overdue
    - name: dateStart
      value: 2025-01-01
      type: query
      description: YYYY-MM-DD
    - name: dateEnd
      value: 2025-09-01
      type: query
      description: YYYY-MM-DD
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
