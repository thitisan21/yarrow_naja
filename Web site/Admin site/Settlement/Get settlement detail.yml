info:
  name: Get settlement detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{url_local}}/backoffice/settlement/detail/:id"
  params:
    - name: id
      value: 67122bb8219a97548e27f989
      type: path
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
