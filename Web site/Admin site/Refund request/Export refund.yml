info:
  name: Export refund
  type: http
  seq: 5

http:
  method: GET
  url: "{{url_local}}/backoffice/admin/refund/export/list?status=refunded&dateStart=2025-01-01&dateEnd=2025-09-01"
  params:
    - name: status
      value: refunded
      type: query
      description: all, request_refund, refunded
    - name: dateStart
      value: 2025-01-01
      type: query
      description: YYYY-MM-DD
    - name: dateEnd
      value: 2025-09-01
      type: query
      description: YYYY-MM-DD
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
