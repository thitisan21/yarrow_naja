info:
  name: List top spender
  type: http
  seq: 2

http:
  method: GET
  url: "{{url_local}}/backoffice/customer/list/top-spender?type=all"
  params:
    - name: type
      value: all
      type: query
      description: // all, year, month, week, today
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
