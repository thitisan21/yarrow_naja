info:
  name: Get customer detail
  type: http
  seq: 4

http:
  method: GET
  url: "{{url_local}}/backoffice/customer/detail/:id"
  params:
    - name: id
      value: 66e3e0ec805b831c284f7b25
      type: path
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
