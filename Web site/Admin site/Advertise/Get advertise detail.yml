info:
  name: Get advertise detail
  type: http
  seq: 5

http:
  method: GET
  url: "{{url_local}}/backoffice/ads/detail/:adsId"
  params:
    - name: adsId
      value: 6917cf115f5a4625633b0d01
      type: path
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
