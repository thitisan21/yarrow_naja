info:
  name: Create settlement per week
  type: http
  seq: 3

http:
  method: GET
  url: "{{url_local}}/backoffice/create/settlement/week"
  params:
    - name: search
      value: ""
      type: query
      description: // run every monday of week => 05 0 * * MON
      disabled: true
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
