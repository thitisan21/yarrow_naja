info:
  name: Check over due
  type: http
  seq: 8

http:
  method: GET
  url: "{{url_local}}/backoffice/settlement/overdue"
  params:
    - name: ""
      value: ""
      type: query
      description: // run every mins , * * * * *
      disabled: true
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
