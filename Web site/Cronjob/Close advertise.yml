info:
  name: Close advertise
  type: http
  seq: 6

http:
  method: GET
  url: "{{url_local}}/backoffice/close/ads"
  params:
    - name: ""
      value: ""
      type: query
      description: // run every midnight , 0 0 * * *
      disabled: true
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
