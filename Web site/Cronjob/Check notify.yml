info:
  name: Check notify
  type: http
  seq: 9

http:
  method: GET
  url: "{{url_local}}/backoffice/notify/event"
  params:
    - name: ""
      value: ""
      type: query
      description: // run every mins , * * * * *
      disabled: true
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
