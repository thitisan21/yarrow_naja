info:
  name: Create settlement per month
  type: http
  seq: 4

http:
  method: GET
  url: "{{url_local}}/backoffice/create/settlement/month"
  params:
    - name: search
      value: ""
      type: query
      description: // 05 0 1 * * run every month
      disabled: true
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
