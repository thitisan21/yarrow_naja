info:
  name: Check waiting list
  type: http
  seq: 1

http:
  method: GET
  url: "{{url_local}}/backoffice/check/waiting/list"
  params:
    - name: search
      value: ""
      type: query
      description: // run every mins ex => * * * * *
      disabled: true
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
