info:
  name: Update status to ended
  type: http
  seq: 5

http:
  method: GET
  url: "{{url_local}}/backoffice/update/status/ended"
  params:
    - name: ""
      value: ""
      type: query
      description: // run every 5 min. 5 * * * *
      disabled: true
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
