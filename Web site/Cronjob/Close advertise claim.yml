info:
  name: Close advertise claim
  type: http
  seq: 7

http:
  method: GET
  url: "{{url_local}}/backoffice/close/claim/ads"
  params:
    - name: ""
      value: ""
      type: query
      description: // run every mins , * * * * *
      disabled: true
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
